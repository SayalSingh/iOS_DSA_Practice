#include <stdio.h>

/* Question 1
int main() {
    int a, i, fact = 1;
    printf("Enter the no. you need factorial: ");
    scanf("%d", &a);
    for (i= 1; i <= a; i++) {
        fact *= i;
    }
    printf("%d",fact);
    

    return 0;
}

*/

/* Question 2

int main() {
   int n;
   printf("Enter no. of elements in array : ");
   scanf("%d",&n);
   
   int a[n], i;
   printf("Enter elements for the array: ");
   for (i = 1; i <= n; i++){
       scanf("%d", &a[i]);
   }
    printf("Your array is ");
   for (i = 1; i <= n; i++) {
     printf("%d ", a[i]);
   }
   
   int sum = 0;
   printf("\n");
  for (i = 1; i <= n; i++) {
      sum += a[i];
  }
  
  printf("The sum of the array is %d", sum);
   
   return 0;
   
}
*/

/* Question 3

*/